# Measured results

<!-- Generated by `node scripts/measure-demo.mjs`. Do not edit by hand. -->

Measured on 2026-09-21T19:31:00.891Z · Node v22.23.2 · darwin arm64
Target: apps/demo-target (the reference application in this repository)

## Browser vs GhostAPI

5 timed run(s) per path, after one untimed warm-up. Figures are medians; the range follows in brackets.

|              | browser (recorded UI replay) | ghostapi (direct API) |
| ------------ | ---------------------------: | --------------------: |
| median time  |         1826 ms (1267–11212) |      **10 ms (5–22)** |
| interactions |                            5 |                     0 |
| requests     |                           30 |                     1 |
| model calls  |                            0 |                     0 |
| tokens       |                _unavailable_ |                     0 |

**182.6× faster through the discovered API.**

> The browser figure is a scripted replay of the recorded UI steps with no model in the loop. A real browser agent adds model latency and tokens on top, so this comparison understates the gap rather than inflating it.
> Each path runs once untimed to warm up, then the reported figure is the median of the timed runs. A single cold run is dominated by process warm-up, and a mean lets one outlier decide the headline.
> Zero model calls on the GhostAPI path: `run` executes a typed operation directly.

## Discovery

| candidate endpoint groups |   8 |
| ------------------------- | --: |
| operations derived        |   8 |
| createTodo confidence     | 94% |
| createTodo observations   |   5 |

Operations: `archiveTodo`, `createTodo`, `deleteTodo`, `getSession`, `listProjects`, `listTodos`, `signIn`, `updateTodo`

Inferred inputs for `createTodo`: `priority`, `projectId`, `title`

## Single executions

| `ghostapi run createTodo`                        | http · 201 · 11 ms                             |
| ------------------------------------------------ | ---------------------------------------------- |
| `ghostapi ask "create a todo called buy coffee"` | heuristic → `createTodo` at 90.0% · 201 · 7 ms |

## Evals

7 passed, 0 failed, 5 skipped across 12 cases and 8 operations.

Reliability over the cases that ran: **100%**.
