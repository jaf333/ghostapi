enable-pre-post-scripts=true

# Pin the public registry for this repository. Without it, whatever registry a
# contributor has configured globally decides where GhostAPI installs from and,
# worse, where `pnpm -r publish` would send these packages.
registry=https://registry.npmjs.org/
